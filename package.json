{
  "name": "ben-am",
  "version": "1.0.0",
  "description": "Alexa skill + web app that lets friends pick your wake-up music and record personalized DJ messages.",
  "private": true,
  "license": "MIT",
  "author": "Ben Ma <ben@benma.dev>",
  "scripts": {
    "install:all": "cd backend/alexa-skill && npm install && cd ../api && npm install && cd ../youtube-dl && npm install && cd ../../frontend && npm install",
    "build": "npm run build:backend && npm run build:frontend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend/alexa-skill && npm run build && cd ../api && npm run build && cd ../youtube-dl && npm run build",
    "lint": "eslint . --ext .ts,.tsx,.js",
    "dev": "cd frontend && npm run dev",
    "deploy:infra": "cd infra && terraform init && terraform apply -auto-approve",
    "clean": "rm -rf node_modules backend/*/node_modules frontend/node_modules infra/node_modules backend/*/dist frontend/dist frontend/build",
    "format": "prettier --write .",
    "update:yt-dlp": "cd lambda-layers && ./build-yt-dlp-layer.sh",
    "deploy:yt-dlp-layer": "cd lambda-layers && LATEST_VERSION=$(curl -s https://api.github.com/repos/yt-dlp/yt-dlp/releases/latest | grep '\"tag_name\"' | sed -E 's/.*\"([^\"]+)\".*/\\1/') && aws lambda publish-layer-version --layer-name yt-dlp-binary --zip-file fileb://yt-dlp-layer.zip --compatible-runtimes nodejs20.x --region us-east-1 --description \"yt-dlp $LATEST_VERSION - $(date +%Y-%m-%d)\" && echo '' && echo '✅ Layer uploaded! Update infra/lambda.tf with the new layer ARN, then run: terraform apply'",
    "update:yt-dlp-full": "npm run update:yt-dlp && npm run deploy:yt-dlp-layer"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0"
  }
}